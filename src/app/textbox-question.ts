import { QuestionBase } from './question-base';

export class TextboxQuestion extends QuestionBase<string> {
  controlType = 'textbox';  // textbox its too big, the odea is jusr to add a placename 19-04-2021
}
